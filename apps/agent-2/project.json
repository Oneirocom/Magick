{
  "name": "agent-2",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/agent-2/src",
  "// targets": "to see all targets run: nx show project agent --web",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd apps/agent-2 && pwd && nitro dev --port 4050"
      }
    },
    "prepare": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd apps/agent-2 && pwd && nitro prepare"
      }
    },

    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["prepare"],
      "options": {
        "command": "cd apps/agent-2 && pwd && nitro build"
      }
    },

    "generate": {
      "command": "npx prisma generate --schema ./prisma/schema.prisma",
      "options": {
        "cwd": "apps/agent-2"
      }
    },

    "serve": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "options": {
        "command": "cd apps/agent-2 && node ./.output/server/index.mjs"
      }
    }
  }
}
