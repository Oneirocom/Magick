{
  "name": "agent-server",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/agent-server/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/agent-server",
        "main": "apps/agent-server/src/main.ts",
        "tsConfig": "apps/agent-server/tsconfig.app.json",
        "assets": ["apps/agent-server/src/assets"],
        "webpackConfig": "apps/agent-server/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "agent-server:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "agent-server:build:development"
        },
        "production": {
          "buildTarget": "agent-server:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"]
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/agent-server/jest.config.ts"
      }
    }
  },
  "tags": []
}
