build:
  docker:
    worker: Dockerfile
run:
  web:
    command:
      - start-server
  manager:
    command:
      - start-cloud-agent-manager
    image: worker
  worker:
    command:
      - start-cloud-agent-worker
    image: worker
