build:
  docker:
    web: Dockerfile
run:
  web: start-server
  manager:
    command:
      - start-cloud-agent-manager
    image: web
  worker:
    command:
      - start-cloud-agent-worker
    image: web
