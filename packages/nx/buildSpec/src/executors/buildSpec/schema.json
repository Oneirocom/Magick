{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "title": "BuildSpec executor",
  "description": "",
  "type": "object",
  "properties": {
    "specFileLocation": {
      "type": "string",
      "description": "The location where the node spec file should be built, relative to the root of the repository."
    },
    "specFileName": {
      "type": "string",
      "description": "The name of the node spec file."
    }
  },
  "required": []
}
