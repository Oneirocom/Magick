import { windowAgentChannels } from './window-agent-channels'

describe('windowAgentChannels', () => {
  it('should work', () => {
    expect(windowAgentChannels()).toEqual('window-agent-channels')
  })
})
